{{- $pages := where .Site.RegularPages "Section" "posts" -}}
[{{ range $i, $p := $pages }}{{ if $i }},{{ end }}{"title":{{ $p.Title | jsonify }},"permalink":{{ $p.RelPermalink | jsonify }},"summary":{{ $p.Summary | jsonify }},"tags":{{ $p.Params.tags | jsonify }}}{{ end }}]